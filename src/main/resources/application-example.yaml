spring:
  application:
    name: mobile-money

  datasource:
    url :
    username:
    password:


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view:

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true